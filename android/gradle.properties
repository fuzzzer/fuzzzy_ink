org.gradle.jvmargs=-Xmx4096M -XX:MaxMetaspaceSize=1024M
android.useAndroidX=true
android.enableJetifier=true

# org.gradle.java.home is intentionally NOT set here: it's a machine-specific
# absolute path, so it belongs in your own ~/.gradle/gradle.properties (never
# committed), not this file. See documents/technical_documentation.md.

# This builtInKotlin flag was added automatically by Flutter migrator
android.builtInKotlin=false
# This newDsl flag was added automatically by Flutter migrator
android.newDsl=false
